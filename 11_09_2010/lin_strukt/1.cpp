#include <iostream>
#include <math.h>
using namespace std;

int main(){
  double a,b;
  cin>>a>>b;
  cout<<"d: "<<(sqrt(pow(a,2)+pow(b,2)))<<endl;
  cout<<"O: "<<(2*a+2*b)<<endl;
  cout<<"P: "<<(a*b)<<endl;

  return 0;
}


